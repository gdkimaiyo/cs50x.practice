-- Prerequisites
-- 1. Have SQLite3 installed
-- 2. On the terminal: For csv files, RUN
  -- 1.1. sqlite3
  -- 1.2. .mode csv
  -- 1.3. import 'csv_file.csv' table
  -- 1.4. .schema


-- List all columns
SELECT * FROM customers;
