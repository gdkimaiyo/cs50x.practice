-- MAX, MIN
-- Find the maximum in a table, e.g max price
SELECT ProductID, ProductName, MAX(price) FROM products;

-- Find the minimum in a table, e.g min price
SELECT ProductID, ProductName, MIN(price) FROM products;
